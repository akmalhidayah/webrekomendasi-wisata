@extends('layouts.admin')
@section('title', 'Detail Hasil Rekomendasi')
@section('content')
<a href="{{ route('admin.hasil-rekomendasi.index') }}">&larr; Kembali</a><h1 class="h3 mt-2">Detail Rekomendasi</h1><p>Kode guest: <code>{{ $guestVisitor->kode_guest }}</code></p><div class="row g-4"><div class="col-lg-5"><div class="card shadow-sm"><div class="card-header fw-semibold">Survei Preferensi</div><div class="table-responsive"><table class="table table-sm mb-0"><thead><tr><th>Wisata</th><th>Rating</th></tr></thead><tbody>@forelse ($guestVisitor->surveyPreferensi as $item)<tr><td>{{ $item->wisata?->nama_wisata ?? 'Data wisata tidak tersedia' }}</td><td>{{ $item->rating_awal }}/5</td></tr>@empty<tr><td colspan="2" class="text-muted">Tidak ada data.</td></tr>@endforelse</tbody></table></div></div></div><div class="col-lg-7"><div class="card shadow-sm"><div class="card-header fw-semibold">Hasil Rekomendasi</div><div class="table-responsive"><table class="table table-sm align-middle mb-0"><thead><tr><th>Rank</th><th>Wisata</th><th>Prediksi</th><th>Similarity</th></tr></thead><tbody>@forelse ($guestVisitor->hasilRekomendasi as $item)<tr><td>#{{ $item->ranking }}</td><td>@if ($item->wisata)<a href="{{ route('admin.wisata.show', $item->wisata) }}">{{ $item->wisata->nama_wisata }}</a>@else Data tidak tersedia @endif<br><small class="text-muted">{{ $item->metode }}</small></td><td>{{ number_format($item->nilai_prediksi, 4) }}</td><td>{{ number_format($item->nilai_similarity, 4) }}</td></tr>@empty<tr><td colspan="4" class="text-muted">Tidak ada hasil.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
