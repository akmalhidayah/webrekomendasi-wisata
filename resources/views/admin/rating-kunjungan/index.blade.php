@extends('layouts.admin')
@section('title', 'Rating Kunjungan')
@section('content')
<h1 class="h3 mb-3">Rating Kunjungan</h1><form class="row g-2 mb-3"><div class="col-md-5"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari wisata, guest, atau ulasan"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">Semua status</option>@foreach (['disetujui', 'ditolak', 'pending'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button> <a class="btn btn-light" href="{{ route('admin.rating-kunjungan.index') }}">Reset</a></div></form><div class="card shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Wisata/Guest</th><th>Rating</th><th>Ulasan</th><th>Status</th><th>Tanggal</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse ($ratings as $item)<tr><td><strong>{{ $item->wisata->nama_wisata }}</strong><br><code>{{ $item->guestVisitor?->kode_guest ?? 'Guest dihapus' }}</code></td><td>{{ $item->rating }}/5</td><td>{{ Str::limit($item->ulasan, 70) ?: '-' }}</td><td><span class="badge text-bg-{{ ['pending' => 'warning', 'disetujui' => 'success', 'ditolak' => 'danger'][$item->status] }}">{{ ucfirst($item->status) }}</span></td><td>{{ $item->created_at->format('d-m-Y') }}</td><td class="text-end text-nowrap"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.rating-kunjungan.show', $item) }}">Detail</a> @if ($item->status !== 'disetujui')<form class="d-inline" method="POST" action="{{ route('admin.rating-kunjungan.setujui', $item) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success">Setujui</button></form>@endif @if ($item->status !== 'ditolak')<form class="d-inline" method="POST" action="{{ route('admin.rating-kunjungan.tolak', $item) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-danger">Tolak</button></form>@endif <form class="d-inline" method="POST" action="{{ route('admin.rating-kunjungan.destroy', $item) }}" onsubmit="return confirm('Hapus rating ini?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-secondary">Hapus</button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Belum ada rating kunjungan.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $ratings->links() }}</div>
@endsection
