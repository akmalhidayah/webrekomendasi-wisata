@extends('layouts.admin')
@section('title', 'Foto Wisata')
@section('content')
<div class="d-flex justify-content-between mb-3"><div><h1 class="h3 mb-0">Foto Wisata</h1><span class="text-muted">{{ $wisata->nama_wisata }}</span></div><a class="btn btn-light" href="{{ route('admin.wisata.show', $wisata) }}">Kembali</a></div>
<div class="card shadow-sm mb-4"><div class="card-body"><h2 class="h5">Unggah Foto</h2><form method="POST" enctype="multipart/form-data" action="{{ route('admin.wisata.foto.store', $wisata) }}">@csrf<div class="row g-3"><div class="col-md-5"><input class="form-control" type="file" name="path_foto[]" accept=".jpg,.jpeg,.png,.webp" multiple required></div><div class="col-md-4"><input class="form-control" name="caption" placeholder="Caption (opsional)"></div><div class="col-md-3"><div class="form-check mt-2"><input class="form-check-input" type="checkbox" name="is_utama" value="1" id="isUtama"><label class="form-check-label" for="isUtama">Jadikan unggahan pertama foto utama</label></div></div></div><button class="btn btn-primary mt-3">Unggah Foto</button></form></div></div>
<div class="row g-3">@forelse ($foto as $item)<div class="col-md-6 col-xl-4"><div class="card shadow-sm h-100">@if($item->foto_url)<img src="{{ $item->foto_url }}" class="card-img-top" style="height:220px;object-fit:cover" alt="{{ $item->caption }}">@else<div class="bg-light d-flex align-items-center justify-content-center text-muted" style="height:220px"><i class="bi bi-image fs-2"></i></div>@endif<div class="card-body">@if ($item->is_utama)<span class="badge text-bg-success mb-2">Foto Utama</span>@endif<form method="POST" enctype="multipart/form-data" action="{{ route('admin.wisata.foto.update', [$wisata, $item]) }}">@csrf @method('PUT')<input class="form-control form-control-sm mb-2" name="caption" value="{{ $item->caption }}" placeholder="Caption"><input class="form-control form-control-sm mb-2" type="file" name="path_foto" accept=".jpg,.jpeg,.png,.webp"><button class="btn btn-sm btn-outline-primary">Simpan</button></form><div class="mt-2">@unless ($item->is_utama)<form class="d-inline" method="POST" action="{{ route('admin.wisata.foto.set-utama', [$wisata, $item]) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-success">Set Utama</button></form>@endunless <form class="d-inline" method="POST" action="{{ route('admin.wisata.foto.destroy', [$wisata, $item]) }}" onsubmit="return confirm('Hapus foto?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hapus</button></form></div></div></div></div>@empty<div class="col-12 text-muted">Belum ada foto tambahan.</div>@endforelse</div>
@endsection
