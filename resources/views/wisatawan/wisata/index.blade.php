@extends('layouts.app')
@section('title', 'Daftar Wisata Makassar')
@section('content')
<div class="container py-5"><h1 class="h2 mb-4">Daftar Wisata Makassar</h1><form class="card card-body shadow-sm mb-4"><div class="row g-2"><div class="col-md-7"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari nama, jenis, atau alamat"></div><div class="col-md-3"><select class="form-select" name="kategori"><option value="">Semua kategori</option>@foreach ($kategori as $item)<option value="{{ $item->id }}" @selected(request('kategori') == $item->id)>{{ $item->nama_kategori }}</option>@endforeach</select></div><div class="col-md-2 d-grid"><button class="btn btn-primary">Cari</button></div></div></form><div class="row g-4">@forelse ($wisata as $item)<div class="col-md-6 col-lg-4"><div class="card h-100 shadow-sm">@if($item->foto_url)<img class="destination-img card-img-top" src="{{ $item->foto_url }}" alt="{{ $item->nama_wisata }}" loading="lazy">@else<div class="destination-img bg-secondary-subtle d-flex flex-column align-items-center justify-content-center text-muted"><i class="bi bi-image fs-2"></i><small>Foto belum tersedia</small></div>@endif<div class="card-body d-flex flex-column"><span class="badge text-bg-info align-self-start mb-2">{{ $item->kategoriWisata->nama_kategori }}</span><h2 class="h5">{{ $item->nama_wisata }}</h2><p class="text-muted small">{{ Str::limit($item->alamat, 90) }}</p><p class="fw-semibold">Estimasi Rp {{ number_format($item->total_estimasi_biaya, 0, ',', '.') }}</p><a class="btn btn-outline-primary mt-auto" href="{{ route('wisatawan.wisata.show', $item->slug) }}">Lihat Detail</a></div></div></div>@empty<div class="col-12"><div class="alert alert-info">Wisata tidak ditemukan.</div></div>@endforelse</div><div class="mt-4">{{ $wisata->links() }}</div></div>
@endsection
